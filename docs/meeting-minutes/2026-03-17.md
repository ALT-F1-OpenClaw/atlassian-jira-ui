# Meeting Minutes — 2026-03-17

**Participants**: Abdelkrim BOUJRAF, OpenClaw AI
**Duration**: ~8 hours (01:00–09:00, 12:00–19:45)
**Location**: Remote (Discord)

## Summary

OAuth 2.0 implementation, dual auth Strategy Pattern, production mode, and deployment debugging.

## Session 1 (01:00–02:00) — Auth Architecture

### Completed
- **v1.55.0**: Per-user session management — OAuth tokens route all API calls
  - `authed_jira_request()` replaces `jira_request()` in all 7 routers
  - Auto-detects OAuth session or falls back to API Token
  - Backend tests updated to mock `authed_jira_request`
- **v1.56.0**: Strategy Pattern for dual auth
  - `OAuthStrategy` + `ApiTokenStrategy` with independent toggles
  - Auth resolver chain: OAuth → API Token → 401
  - Toggle switches in Settings UI (iOS-style)
- **v1.56.1**: Friendly 401 error screen
  - `ErrorDisplay` component: 🔒 icon + Login button + Settings button
  - Replaces raw "Error: 401" text

### Decisions Made
- Both auth methods coexist — OAuth takes priority when session exists
- `APP_ENV` controls auth availability (ADR-017)

## Session 2 (08:00–09:00) — OAuth Debugging

### Issues Found & Fixed
- **v1.56.2**: Traefik didn't route `/auth/*` to backend → added `/auth` routes in `dynamic.yml`
- **v1.56.3**: Missing `manage:jira-project` scope → added to OAuth scopes
- **v1.56.4**: Session cookie rejected by Firefox → `secure=False` behind Traefik TLS termination

## Session 3 (12:00–19:45) — OAuth Testing & Sprint Fallback

### OAuth Flow Testing
- Consent screen works ✅
- Token exchange works ✅
- Agile API returns 401 "scope does not match" ❌
  - Root cause: Jira Software granular scopes need separate product in Developer Console
  - User found and added Jira Software scopes
  - Even after re-login: Agile API still rejects (instance doesn't have Jira Software product)

### Sprint Platform API Fallback (v1.59.0)
- **ADR-016**: When Agile API fails, falls back to Platform API
- JQL: `sprint in openSprints()` → extract from `customfield_10020`
- Same response format — transparent to frontend
- Sprints now visible via OAuth ✅

### Additional Features
- **v1.58.0**: Production mode — API Token completely disabled when `APP_ENV=production`
- **v1.58.1**: Deploy README rewritten with full env comparison
- **v1.58.2**: `APP_ENV` values documented (development/staging/production)
- **v1.58.3**: Auto-refresh expired OAuth tokens before API calls
- **v1.59.1–v1.59.13**: Sprint Jira link fixes (project type detection)
  - Team-managed software: `/jira/software/projects/{key}/boards/{id}`
  - Company-managed software: `/jira/software/c/projects/{key}/boards/{id}`
  - Business: `/jira/core/projects/{key}/board`
  - ADR-018 created

### ADRs Created
- ADR-016: Platform API fallback for sprints
- ADR-017: Dual auth Strategy Pattern
- ADR-018: Jira URL construction rules

## Metrics

| Metric | Start | End |
|--------|-------|-----|
| Version | v1.54.2 | v1.59.13 |
| ADRs | 15 | 18 |
| OAuth scopes | 4 | 10 (platform + software) |
| Auth strategies | 1 (API Token) | 2 (+ OAuth) |
