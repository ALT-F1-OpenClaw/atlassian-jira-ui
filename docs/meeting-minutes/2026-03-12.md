# Meeting Minutes — 2026-03-12

**Participants**: Abdelkrim BOUJRAF, OpenClaw AI
**Duration**: ~5 hours
**Location**: Remote (Discord)

## Summary

SearchableSelect component, Create Project feature, doc updates, and CI fixes.

## Morning Session (09:00–12:10)

### Feature: SearchableSelect Autocomplete Dropdowns (v1.49.0)
- Built custom `SearchableSelect` component with type-to-search
- Replaced 7 native `<select>` elements across the app
- Keyboard navigation: ↑↓ arrows, Enter, Escape
- `autoOpen` prop for inline edit selects (priority, assignee)
- 23 test rewrites needed for new interaction pattern
- Added `scrollIntoView` JSDOM mock
- ADR-011 created

### Feature: Create Submenu — Issue + Project (v1.49.0)
- `+ Create` button now shows dropdown with Issue and Project
- `c` shortcut still opens Issue modal directly
- New `CreateProjectModal`: name, auto-generated key, type, lead, description
- Backend `POST /api/projects` endpoint added
- Key auto-generates from project name initials (e.g., "My Awesome Project" → "MAP")

### CI Fix
- v1.48.2 docs commit — fixed README screenshots pointing to wrong files
- All docs updated: README, AGENTS.md, CLAUDE.md with ASCII CI/CD pipeline diagram

## Decisions Made

1. **SearchableSelect over native `<select>`** — better UX with type-to-filter (ADR-011)
2. **Create submenu pattern** — extensible for future create actions (sprint, etc.)
3. **Auto-generated project key** — first letter of each word, uppercase, max 10 chars

## Action Items

- [x] Fix remaining 2 unit tests (Create button → Create menu → Issue)
- [x] Add backend `POST /api/projects` endpoint
- [x] Update all doc files
- [ ] Update ROADMAP.md with new features (carried to next session)

## Metrics

| Metric | Start | End |
|--------|-------|-----|
| Version | v1.48.0 | v1.49.0 |
| Unit tests | 255 | 255 |
| Dropdowns replaced | 0 | 7 |
| New components | 0 | 2 (SearchableSelect, CreateProjectModal) |
