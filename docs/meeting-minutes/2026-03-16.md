# Meeting Minutes — 2026-03-16

**Participants**: Abdelkrim BOUJRAF, OpenClaw AI
**Duration**: ~1 hour
**Location**: Remote (Discord)

## Summary

Production deployment on Raspberry Pi 4 with Docker, Traefik, and 3 environments.

## Session (16:08–16:35)

### Infrastructure Setup
1. **Docker installed** — Docker Engine v29.3.0 + Compose v5.1.0
2. **Standard mode** (user added to docker group) — not rootless, Pi is on private Tailscale network
3. **Directory structure created**: `/srv/atlassian-jira-ui/{dev,staging,prod,traefik}`
4. **Tailscale TLS cert** generated for `atlf1be-raspberry-pi-4.tail981e59.ts.net`

### Deployment Architecture (ADR-012, ADR-013, ADR-014)
- Traefik v3.4 as reverse proxy with TLS termination
- 3 environments running in parallel via Docker Compose
- Port-based routing (443 reserved for OpenClaw/Tailscale Serve)

| Environment | Port | Container Names |
|-------------|------|----------------|
| Prod | 4443 | prod-frontend, prod-backend |
| Staging | 8443 | staging-frontend, staging-backend |
| Dev | 9443 | dev-frontend, dev-backend |
| Traefik | 8080 | traefik |

### Issues Encountered & Resolved
1. **Port 443 conflict** — Tailscale Serve uses 443 for OpenClaw → switched prod to 4443
2. **Frontend nginx "host not found"** — nginx.conf references `backend` but containers named `prod-backend` → created per-environment nginx.conf with correct upstream names

### Files Created
```
/srv/atlassian-jira-ui/
├── docker-compose.yml          ← 7 services
├── traefik/
│   ├── traefik.yml             ← 4 entrypoints (prod/staging/dev/dashboard)
│   ├── dynamic.yml             ← 6 routers + 6 services
│   └── *.crt/*.key             ← Tailscale TLS
├── prod/.env + nginx.conf
├── staging/.env + nginx.conf
└── dev/.env + nginx.conf
```

### Documentation Created
- 15 ADRs covering all major architectural decisions
- Meeting minutes for 2026-03-11, 2026-03-12, 2026-03-16
- Systemd service file for auto-start on reboot

## Decisions Made

1. **Docker over bare metal** — reuses GHCR images, isolated environments (ADR-012)
2. **Port-based over subdomain** — Tailscale certs don't cover subdomains (ADR-013)
3. **Tailscale TLS over Let's Encrypt** — private network, no public DNS (ADR-014)
4. **Prod on :4443** — port 443 reserved for OpenClaw

## Action Items

- [x] Install Docker
- [x] Create /srv directory structure
- [x] Generate Tailscale TLS certs
- [x] Write Traefik + Docker Compose configs
- [x] Fix nginx upstream naming
- [x] All 7 containers running
- [ ] Enable systemd service (pending sudo from user)
- [ ] Wire CI/CD auto-deploy on push/tag
- [ ] Document deployment in README

## Metrics

| Metric | Value |
|--------|-------|
| Containers running | 7 |
| Environments | 3 (dev, staging, prod) |
| RAM usage | ~500MB total (estimated) |
| Version deployed | v1.49.0 |
| Time to deploy | ~25 min (including Docker install) |
